#!/bin/sh
echo "Running dotnet format check..."
dotnet format AmeCapture.slnx --verify-no-changes --severity error
if [ $? -ne 0 ]; then
  echo ""
  echo "Format check failed. Run 'dotnet format AmeCapture.slnx' to fix formatting, then commit again."
  exit 1
fi
